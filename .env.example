# 🌐 CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:4200

# 🛠️ Session
SESSION_SECRET=superSecretSessionKey
SESSION_NAME=session_id
SESSION_MAX_AGE=86400000 # en milisegundos (1 día)
SESSION_SECURE=false # true en producción con HTTPS

# 🗄️ Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=myapp
DB_SSL=false # true si usas SSL (e.g. en producción con render/railway)

# 🚀 App
PORT=3000
